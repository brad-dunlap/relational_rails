<h1>Menu Items</h1>
<% @items.each do |item| %>
	<h3><%= link_to item.name, "/items/#{item.id}" %></h3>
	<p1><%= link_to "Edit #{item.name}", "/items/#{item.id}/edit", method: :get %>
	<%= link_to "Delete #{item.name}", "/items/#{item.id}", method: :delete %></p1>
<% end %>

